'use strict';

/**
 *  random-question-array controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::random-question-array.random-question-array');
