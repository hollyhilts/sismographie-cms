'use strict';

/**
 * random-question-array service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::random-question-array.random-question-array');
